name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build . --rm --no-cache --file Dockerfile --tag docker-youtube-dl:$(date +%s)
